# Pinned, vetted Julia env (Piccolo 1.19) bundled in the VSIX.
# Provisioned to ~/.amico/julia via `Pkg.instantiate()` (scripts/install.sh).
# Regenerate: re-instantiate ~/.amico/julia, then copy Project.toml + Manifest.toml here.
